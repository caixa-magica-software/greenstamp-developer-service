const path = require("path")

module.exports = {
  config: path.resolve(__dirname, "src", "data", "mysql", "config", "config.js"),
  "migrations-path": path.resolve(__dirname, "src", "data", "mysql", "migrations") 
}